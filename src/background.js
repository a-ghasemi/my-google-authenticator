// Example background script
browser.runtime.onInstalled.addListener(() => {
    console.log("Extension installed");
    // Perform any setup tasks here
});

// Add more listeners and functions as needed
